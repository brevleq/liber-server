version: '2'
services:
  liber-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/liber/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=liber
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
